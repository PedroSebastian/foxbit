name: foxbit_client
description: A new super Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  animate_do: ^2.0.0
  connectivity: ^3.0.3
  flutter:
    sdk: flutter
  flutter_clean_architecture: ^5.0.0
  flutter_money_formatter: ^0.8.3
  flutter_svg: ^0.19.3
  rxdart: ^0.26.0
  websok: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^1.5.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/